# Vite exposes env vars prefixed with VITE_
# Local dev usually only needs VITE_API_URL. If VITE_WS_URL is omitted,
# the client derives it automatically as <VITE_API_URL>/ws.
VITE_API_URL=http://localhost:4000
